# TODO

- Add git hooks pre-commit with linters from puppet training vm
- Add parameter to set the number of jruby's of a puppetserver JVM
- Add more beaker tests
- Add more rspec-puppet tests
- Add native support for Debian 8 and Debian 9 with JAVA packages
- Create a new context with puppetdb=>true on the beaker tests
- Create a parameter server_config like puppetlabs/mysql/override_defaults
- Remove puppetdb_termini management and use puppetlabs/puppetdb instead
- Create parameters to install agent using instruct-br/puppetagent